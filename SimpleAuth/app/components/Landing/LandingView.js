import React from 'react';
import ReactDOM from 'react-dom';

import * as CONSTANTS from '../../common/constants';

const Landing = () => {
	return (
	<div className="container">
    <h2>{CONSTANTS.PAGE_LANDING}</h2>
	</div>
	)
}

export default Landing;
